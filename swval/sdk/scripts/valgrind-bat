#!/bin/bash
# INTEL CONFIDENTIAL - For Intel Internal Use Only

shopt -o -s nounset

declare -r SCRIPT_NAME='valgrind-bat'

. ./valgrind-common

declare ARGS=''

declare WANTDISTCC=false
while [ $# -gt 0 ]; do
   case $1 in
      --clean)
         clean_valgrind_all "tests/harnessed/gtest/.libs/bat"
         exit 0
      ;;
      --distcc)
         WANTDISTCC=true
      ;;
      *)
         ARGS="${ARGS} $1"
      ;;
   esac
   shift
done

run_valgrind_all ${WANTDISTCC} "tests/harnessed/gtest/.libs/bat" ${ARGS}
exit $?
